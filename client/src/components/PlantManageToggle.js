import React from "react";
import styled from "styled-components";
const Layout = styled.div``;

function PlantManageToggle({ className }) {
  return <Layout className={className}></Layout>;
}

export default PlantManageToggle;
